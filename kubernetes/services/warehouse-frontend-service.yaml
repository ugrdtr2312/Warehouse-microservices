--- # ---

# apiVersion: v1
# kind: Service
# metadata:
#   name: warehouse-front-service
# spec:
#   selector:
#     app: warehouse-front-pod
#   ports:
#     - port: 8080
#       targetPort: 80

apiVersion: v1
kind: Service
metadata:
  name: warehouse-front
spec:
  selector:
    app: warehouse-front
    tier: frontend
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 80
