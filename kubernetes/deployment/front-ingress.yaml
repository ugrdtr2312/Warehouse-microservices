apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: warehouse-front-ingress
spec:
  rules:
    - host: minikube
      http:
        paths:
          - path: /

            pathType: Prefix
            backend:
              service:
                name: warehouse-front-service
                port:
                  number: 8080
