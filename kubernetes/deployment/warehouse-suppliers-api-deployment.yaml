apiVersion: apps/v1
kind: Deployment
metadata:
  name: warehouse-suppliers-api-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: warehouse-suppliers-api-pod
  template:
    metadata:
      labels:
        app: warehouse-suppliers-api-pod
    spec:
      containers:
        - name: warehouse-suppliers-api-container
          image: warehouse-suppliers-api:latest
          imagePullPolicy: Never
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - containerPort: 80
