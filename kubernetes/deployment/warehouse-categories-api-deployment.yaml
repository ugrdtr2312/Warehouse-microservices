apiVersion: apps/v1
kind: Deployment
metadata:
  name: warehouse-categories-api-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: warehouse-categories-api-pod
  template:
    metadata:
      labels:
        app: warehouse-categories-api-pod
    spec:
      containers:
        - name: warehouse-categories-api-container
          image: warehouse-categories-api:latest
          imagePullPolicy: Never
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - containerPort: 80
